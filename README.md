# Actions Runner Controller (ARC) Multi-Cloud Build

## Overview
The ARC or Actions Runner Controller is an orchestrator of self-hosted Github Actions runners hosted in Kubernetes.

This project aims to create the ARC infrastructure based on the cloud provider of choice or as required. The workflow uses Terraform, Bash, and custom Github Actions to provision the infrastructure.

## Workflow